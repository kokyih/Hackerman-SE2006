<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ce2006.schoolconnect"
    android:targetSandboxVersion="1"
    android:versionCode="1"
    android:versionName="1.0">

    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:configChanges="keyboardHidden|orientation"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SchoolConnect"
        android:usesCleartextTraffic="true">
        <!--<meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/activity_maps" /> -->

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- All Product Activity -->
        <activity
            android:name=".MainMenu"
            android:label="Main Menu" /> <!-- Add Product Activity -->
        <!-- Edit Product Activity -->
        <activity
            android:name=".RegistrationActivity"
            android:label="Register User" />
        <activity
            android:name=".GPSActivity"
            android:label="For GPS" />
        <activity
            android:name=".UploadFile"
            android:label="Upload file" />
        <activity
            android:name=".DownloadFile"
            android:label="For Download" />

        <activity
            android:name=".SubmitFeedback"
            android:label="For feedback submission" />

        <activity
            android:name=".ViewFeedback"
            android:label="For viewing feedback" />

        <activity
            android:name=".ViewSpecificFeedback"
            android:label="For viewing 1 feedback" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${mapsApiKey}" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>